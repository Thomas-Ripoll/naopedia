<div id="profil">
     <ul class="navbar-nav">
{% if app.user %}
    <li class="nav-item">
        <a class="nav-link" href="{{path('userpage',{'username':app.user.username}  )}}">Mon profil</a>
    </li>
    {% if is_granted('ROLE_ADMIN')%}
        <li>
            <a class="nav-link" href="{{path('admin')}}">Administration</a>
        </li>
    {% endif %}

    <li class="nav-item">
        <a class="nav-link" href="{{path('post')}}">Poster une observation</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="{{path('logout')}}">Se déconnecter</a>
    </li>
{% else %}
    <li class="nav-item">
        <a class="nav-link" href="{{path('login')}}">Se connecter</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="{{path('signin')}}">S'inscrire</a>
    </li>
{% endif %}
     </ul>
</div>
